---
name: film-highlight-editor
description: 把完整影视剧剪成"蒙太奇骨架+深度解说转场"精华版并交付B站三件套（v14：单会话全流程批处理——一个会话从读片到交付跑完每部片，不再分max/flash双模型，废handoff.json/batch_queue.json队列与文件传递协议；靠每片磁盘产物实现失败隔离、零提问、断点可续；工业化生产全部收敛于包内 projects/按片名分目录（source/work/output 三级），换机零改路径即可开源）。触发词：精华版、影视解说剪辑、四幕剪辑、转场解说、B站影视区交付、批量剪片、自动剪辑。
agent_created: true
---

# 影视精华版剪辑流水线（v14 单会话全流程批处理 | 能力基线 v13.4 全部保留）

## 技能包目录（本文件夹，工业化布局，v14 定版）
```
影视精华版-film-highlight-editor/     ← 技能包＝只读引擎＋文档，禁止往里写运行产物
├── SKILL.md            ← 本协议（唯一权威，旧副本已清除）
├── scripts/            ← 全部正式脚本（20个，见工具清单）
├── brand/              ← 品牌资产（render_brand_v2.py 定版 + 预渲染 intro/outro/wm 三档 mp4/png）
├── models/             ← vosk 中文小模型 vosk-zh（断网兜底 ASR 用）
├── projects/           ← ★工业化生产总目录（每部片一个子文件夹，见下）
│   ├── batch_list.md   ← 跨会话任务台账（片名｜源路径｜状态｜备注）
│   └── <片名>/          ← 按片名命名、短而稳（如"抓特务"）
│       ├── source/     ← 输入素材：台词本.txt(全片ASR)、故事脉络.md、新闻源/调研笔记、h3/提示词等
│       ├── work/       ← 中间产物：sheets/ cutprobe/ prologue/ trans/ cover/ tts/ clips/ qc/
│       └── output/     ← 交付：`<片名>_精华版_<日期>.mp4`、`_封面_<日期>.jpg`、`_发布包_<日期>.md`、剪辑报告.md、验收报告.md
└── _backup_20260906/   ← 2026-09-06 整改快照（v13.2/v13.5 协议、旧脚本、废弃的 max/flash 提示词），确认稳定后可整体删除
```

**工业化生产目录 = 包内 `projects/<片名>/`**（结构 source/｜work/｜output/ 三级，见上图）。片源本体不进包（GB 级大文件），放用户自备片源目录，其路径记入 `projects/batch_list.md` 台账与 `source/素材清单.txt`。台账 `projects/batch_list.md`：Markdown 表（片名｜源路径｜状态[待办|进行中|done|blocked|跳过]｜备注），新会话续跑第一步读它。
## 版本沿革（v10→v14，只列本版仍需生效的结论）
- **v11**：品牌三件套（片头前贴/每片中缝/片尾尾贴，brand/ 预渲染 mp4 直接取用，禁叠字幕）；B站交付三件套（成片+封面+发布包）；发布文案强制联网核实；字幕三红线（禁提前/滞后/重叠）
- **v12**：①每段解说≤60s；②首句=剧名开头；③幕转场四选一（设问/悬念/递进/反差）；④骨架硬切+中缝，禁淡入淡出划像；⑤转场卡0.5s大缓冲；⑥**解说声全程垫底不断**（正片蒙太奇段也用垫底解说，v13.3 允许音量分层：骨架垫底 -20dB、卡与解说 -13~-15dB）；⑦**禁旁白报时间码**（"第46分22秒"等一律禁止，方位感用人话）；⑧封面纯图禁字
- **v13**：①**故事脉络片头 38-45s**（render_prologue.py）；②**解说卡上限 105-115s**；③**TTS 音色可定制**（§2.7）；④**每幕≤90s、正片目标350-420s、总长目标400-480s**；⑤**验收自动化**（verify_sync.py+tail_guard.py，§9.2）；⑥**联网核实强制化**；⑦**时长必实测**（§0 探针）；⑧发布包禁提模型/技能名；⑨**中缝硬要求：相邻≥2张卡之间必插卡，插卡后解说顺延不断声**（§7.3/9.3）；⑩**正片禁"第一幕/第二幕"报幕词**，幕边界只用卡自然切
- **v13.1**：⑪**片尾彩蛋禁入剪辑范围**（§8 三招）；⑫**骨架每镜 10-20s、每幕 70-80s**（§3）；⑬**封面禁黑帧**（§9.4）；⑭**字幕防重叠**（§9.5）；⑮**发布包简介 150-220 字**（§10）
- **v13.2**：⑯**品牌定版锁死**（brand/render_brand_v2.py 唯一实现，产物 intro/outro_{720p,1080p,4k}.mp4 已预渲染，直接取用禁再改）；⑰**每部片必须从片头开始完整重看一遍**（§0 硬性步骤，禁凭海报/记忆/旧稿剪辑）；⑱**骨架蒙太奇禁加字幕**——正片段只留原声画面，解说卡与片头卡才挂 SRT；assemble 只喂卡段 SRT；§9 验收加"正片段零字幕"
- **v13.3**：⑲**骨架必须多场次/多场景组接**（§2.4）——每镜来自不同场次/景别，每幕 ≥4 个不同场次，连续同场景 ≤2 镜；`contact_sheet.py --scenes` 场景切分先验
- **v13.4**：⑳**解说文本必须独立成文**（§6.0）——先当一篇文章写（250-350字/张卡，钩子→展开→金句/悬念，禁口水话），后贴画面；贴画面只做时长裁剪不做结构让步；QC 念读测试
- **v14.1**：㉒**配音定版百炼**（qwen3-tts-flash/Bellona 燕铮莺，禁浏览器 TTS；实测 cosyvoice-v3.5 无 Bellona，418）；㉓**生产目录收敛包内 projects/<片名>/{source,work,output}**（对齐 snap_frame 先例），废外部工作区；㉔新增 `tts_bailian.py`+`full_asr.py`（脚本 20 个）
- **v14**：㉑**单会话全流程批处理**——废除 max/flash 双模型分工与 handoff.json/batch_queue.json 交接协议（双提示词 txt 已删）；每部片一个会话从头跑到尾，跨批续跑靠 batch_list.md 台账＋每片磁盘产物；生产目录收敛包内 projects/；技能包文件夹工业布局（docs/ 归位、旧副本与死脚本清除）

## 0. 第一步：先读电影（**v13.2 硬性步骤，每部片必做，禁跳过**）
**拿到任何片子，第一件事是完整看一遍——从片头开始，不是抽查。** 即使该电影/剧集此前剪过（v12 试剪存在），也必须从片头重新完整看一遍再动剪——剧情记忆、旧剪辑稿、海报宣传语都可能与正片不符；**禁凭海报/记忆/旧稿剪辑**；旧稿最多参考节奏与转场句，镜号一律重新实勘。
- **读片流程**：①`ffprobe` 实测总时长/分辨率/帧率/音轨 → ②`ffmpeg -ss/-t` 每 15-20min 抽 1.5x-2x 片段 + 全片 contact_sheet 缩略图网格，两路都走，**0-10min 开场段用常规速度看** → ③边看边建 `work/sheet/故事脉络.md`：时间轴(秒)+场景+人物+情绪+可剪点、高潮与转折、场次/场景清单（§2.4）
  - **v13 读片前先跑时长探针（§0.1，2 分钟）**：别信文件名/海报/旧笔记的时长。
- **0.1 时长探针（v13 新增）**：`ffprobe -v error -show_entries format=duration` 实测；>40min 按电影处理；<25min 是短集。
- **读完自检**（任一为"否"就继续看）：闭眼能按时间顺序讲 3 分钟？知道每个高潮确切秒数？四幕有足够场景？知道片头正片起点和片尾彩蛋起止？
- 读完再列四幕。**禁只看缩略图/字幕/简介就开剪**；长片读片是分批的——每 40-60min 更新一次笔记。

## 1. 叙事结构：蒙太奇骨架 + 解说卡转场（硬性）
```
[故事脉络片头 38-45s(v13)] → [幕1骨架蒙太奇 ~75s,原声] → [转场卡① 0.5s大缓冲 + 解说卡 ~95s(上限115s)] → [幕2骨架 ~75s] → [卡②] → [幕3骨架 ~75s] → [卡③] → [幕4骨架 ~75s] → [卡④ ~60-95s] → [品牌尾贴+防剧透尾巴]
```
- 骨架每镜 **10-20s、每幕 70-80s（v13.1）**；转场卡=下一幕浓缩；**每段解说≤60s**；**每幕≤90s、正片目标350-420s、总长目标400-480s（v13）**；**卡与卡之间必须有骨架段（防连排）**；**解说全程垫底**；骨架用原声（垫底解说-20dB 下）；卡上禁原声；**正片禁报幕词**；**骨架禁字幕（v13.2）**；**骨架多场次（v13.3）**；**解说文本独立成文（v13.4）**。

## 2. 取材规则（全部来自原片）
### 2.1 禁 AI 生成画面（零容忍）
### 2.2 禁读台词/字幕（硬规则）
解说词不得照读/复述画面段正在播出的台词或对白字幕（原声与观众同场，复读=割裂+变相剧透）；解说只讲画面之外的背景、结构与主题。骨架原声台词不进入解说文本；金句若要引用需改写为叙述视角。（v14.1 找回：本条正文在 v14 重写时丢失，按 v13 原意恢复，2026-09-07 抓特务实拍时纠正卡③违规）
### 2.3 场景选取"三问"
### 2.4 场次多样性（v13.3 硬要求）：每幕 ≥4 个不同场次；连续同场景 ≤2 镜；`contact_sheet.py --scenes` 先验。
### 2.5 片尾彩蛋三招（v13.1）：看尾段 3-5min 标彩蛋起止；片头起点保守；骨架/卡素材 `end ≤ 正片结束-60s`。
### 2.6 品牌与封面禁字（v12）：封面无"4K"水印残留。

## 2.7 配音引擎（v14.1 定版：百炼 TTS 主力）
**本技能包 TTS 引擎 = `scripts/tts_bailian.py`（百炼 qwen3-tts-flash，音色 Bellona=官方"精品百人-燕铮莺"）**。禁用浏览器/系统自带 TTS。用法：`python tts_bailian.py <texts.json> [--outdir tts] [--voice Bellona] [--speed 1.0]`。texts.json=[{"name","text"}...]（兼容 narration_plan.json 的 {"narrations":[{...}]}）；输出 <name>.wav + <name>.dur.json（实测时长回写）+ tts_manifest.json；已建 ttsout 跳过（断点续传）、空输出自动重试、429/超时指数退避。
**引擎事实（2026-09-07 实测）**：①"燕铮莺 Bellona"挂在 **qwen3-tts 线**（同 key 同账号），不在 cosyvoice 系统音色表——cosyvoice-v3.5-plus 用 Bellona 报 418；②qwen3-tts 流式返回多帧独立 base64 wav（头长不固定），脚本已按逐帧解码+剥头拼 PCM 重封装；③每请求一段文本（API 无逐段时长字段，时长由下游 ffprobe 实测）；④要换 cosyvoice-v3.5 克隆声（推荐网友定制专属音色）：先经 DashScope voice-enrollment 复刻（3-10s 样本得 voice_id），但该线走 SpeechSynthesizer 协议、与本脚本 MultiModalConversation 不同——tts_bailian.py 仅实测覆盖 qwen3-tts 线，cosyvoice 通道参考语音克隆技能包/官方文档接入。注意两线音色表不通用（Bellona 只在 qwen3-tts 线）。
**选声（2026-09-07 用户听样定版）**：默认 **Bellona（燕铮莺）**，开源推荐语同步写进 README——"默认推荐百炼 qwen3-tts 的『燕铮莺 Bellona』，另推荐网友用 cosyvoice-v3.5 做声音复刻定制专属音色"（cosyvoice-v3.5 复刻：voice-enrollment 得 voice_id → SpeechSynthesizer 协议接入，见上条引擎事实④）。**同一片从头到尾只用一个音色（禁中途换）**；**TTS 文本禁破折号**（部分引擎读不出来）。
**依赖**：`dashscope` 不装——脚本用 stdlib requests 直调 REST；requests 在便携 venv 已存在。API key 解析顺序：env DASHSCOPE_API_KEY → 包内 scripts/.env → U 盘根 .env（HERMES_CUSTOM_AISIA_CFY_BAILIAN_API_KEY），**开源发布前确认 .env 不进包**。

## 3. 骨架蒙太奇选段
- **每镜 10-20s、每幕合计 70-80s**；`cutprobe_sheet.py` 双边界探针；**snap 到镜头切点（snap_cuts.py）**。

### 3.5 故事脉络片头（v13，render_prologue.py，品牌定版禁改）
`--style title|scene|black`；title 1080p 片名/黑色渐变，scene 原画暗角压 15% 亮度，black 纯黑底；旁白四要素：梗概<45字/主角身份/冲突转折/结局悬念；镜头库 8-15 镜候选；总 38-45s。

## 4. 转场卡
- **转场卡 0.5s（v12）**；解说卡 95-115s（v13 上限 115s）；首句剧名；四选一钩子；不剧透；**禁报时间码**；**禁"第X幕"报幕词**；**文本独立成文（v13.4）**。

## 5. 解说词（转场卡）
**先成文后贴画面（v13.4 硬性）**：①每张卡先是一篇 250-350 字文章（钩子→展开→金句/悬念收尾）；②再按画面节奏分段对齐。念读测试。

## 6. 剪辑与组装
### 6.0 解说文本独立成文（v13.4）——见 §5
### 6.1 时间线（音频驱动）
每段解说卡=若干 tts 子段 wav 以 0.35s 静音间隔拼接（首尾各留 0.5/1.0s 呼吸），`render_transition.py` 按音频实测时长反推滚动速度，画面服从音频；骨架段时长服从切点吸附（snap_cuts），不得为凑总长硬剪。拼接顺序唯一准据=`work/order.txt`（纯路径每行一个，禁行内注释）。

### 6.2 画面
全部素材来自原片（§2.1 禁 AI 画面）。源片画幅≠1080p 时切片必须 letterbox 统一：`cut_sync.py --vf "scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2:black,fps=25"`（v14.1 新增 --vf 透传，808/2.35:1 宽银幕实测有效）。转场卡=黑底滚动文字，天然规避源片硬字幕。

### 6.3 音频混制（ad_scan.py 三档 + tail_guard.py）
骨架段保留原声（对白+BGM 一体），解说卡段只有 TTS 干声；**现行交付=交替式**（骨架原声→卡解说，卡上禁原声），"解说全程垫底-20dB"因无混音工具层未启用（2026-09-07 抓特务实测定版，留痕）。中插广告剧用 `ad_scan.py` 三档扫描剔除；交付前 `tail_guard.py` 复检尾巴。

### 6.4 字幕（**只给卡段挂 SRT，骨架零字幕 v13.2**）
骨架段零外挂字幕（源片自带烧录字幕不算，不得再叠）。卡段解说文字由转场滚动呈现，**不再另挂 SRT**；防重叠校验仅当未来外挂时使用（§9.5）。ASR 只作内部通读与金句定位素材（台词本.txt），永不进成片。

### 6.5 组装（assemble.py 五缝合一）
`assemble.py order.txt --final X --mode normalize`：intro+prologue+骨架×4+卡×4+outro 一次帧网格量化拼接（v8 零漂移金标准），装配后自动校验成片帧数==ΣV。交付级一律 normalize，copy 仅预览。


## 7. 执行与迭代
### 7.1 小样先行
### 7.2 时长收敛
### 7.3 防剧透插卡
### 7.4 迭代节奏
### 7.5 批量模式（v14：单会话全流程，废双模型分工协议）
**v14 决定：一个会话做完一部片的全部流程（§0→§13），不再区分强/弱模型、不再切两段跑、不再用 handoff.json/batch_queue.json 传上下文。**

- **路径全部相对技能包根**（不依赖 HERMES_HOME/盘符）：生产目录=包内 `projects/<片名>/{source,work,output}`；片源放用户自备的**片源目录**（与技能包分离，路径记台账，不进包）。禁写宿主目录（%TEMP% 等）。
- **片源约定**：片源目录由使用者指定（`<片源目录>/<片名>/<片名>.mp4`）；**禁在技能包、工作区里放片源**。
- **台账续跑（替代旧队列协议）**：多片批量时先建/更新 `projects/batch_list.md`（Markdown 表：片名｜源路径｜状态[待办|进行中|done|blocked|跳过]｜备注）。**每完成一部立刻改台账状态**；新会话/续跑第一步读台账，找第一个非 done 片，从该片已有磁盘产物继续（§7.6 断点续跑）。旧 batch_queue.json / handoff.json 协议**作废**。
- **会话纪律（通宵无人值守）**：不提问等确认——规则全在协议里；**全程不向用户提问**（用户休息），一切按协议自行决定；缺信息按最合理假设走，风险点写剪辑报告"决策留痕"（§13）；不确定时**宁可保守+留痕**，不停下问。
- **失败隔离（单片失败不许拖垮批次）**：某片任一步失败→先**重试一次**；仍失败→台账标 `blocked: <一句原因>`，**立即跳到下一部**，绝不死等或无限重试；blocked 片留磁盘产物供复核。**重试上限：同一步骤同一会话最多 2 次**。
- **上下文纪律（单会话版，防长跑遗忘）**：长会话读片笔记落盘（`work/sheet/故事脉络.md`）；决策落 `output/剪辑报告.md`；**上下文被压缩/换新会话时，靠磁盘文件恢复**；上下文接近极限而本片未完→把当前步骤写进台账备注+剪辑报告，下会话据此续跑。
- **成本约束**：长片（90min+）读片/抽帧用 1.5x-2x 快放；`snap_cuts.py` 复用 `.sigcache.json` 免二次全片扫描；TTS 用 §2.7 默认百炼 qwen3-tts-flash/Bellona（按量计费，141min 片解说词量约几分钱）；单会话内完成，不做跨模型交接。
- **每部片标准作业序列（就是"流程"0→13，一步不缺）**：目录初始化→读片（§0.1 探针+§0 完整读片）→四幕规划→选镜（§2.4）→骨架→卡解说（§5 独立成文）→TTS（§2.7）→组装（§6）→字幕（§6.4）→§7.3 防剧透插卡→品牌片尾+尾巴（§2.5）→验收（§9.2 自动）→封面（§9.4）→联网核实（§10）→发布包（§10）→台账标 done。
- **§9.2 验收 FAIL → 修复循环（单会话内闭环）**：重测→定位→修复→再验收，**同一片最多 3 轮**；3 轮仍 FAIL→台账记 `done-with-issue: <残留问题>` 并继续下一部。

### 7.6 断点续跑（磁盘产物即进度）
每步产物落盘且命名固定（sheet/、prologue/、trans/、tts/、output/），任何中断后重进：`ls work/ output/` 看已有什么，对照标准作业序列找第一个缺失产物，从那步继续。TTS 断点续传在 tts_batch.py 内建；assemble 可重跑。

## 8. 片尾防剧透
- **剪辑范围止于正片结束（v13.1）**；片尾留白+悬念句；防剧透尾巴。

## 9. 质量验收
### 9.1 人工抽查（抽帧看转场、听音画同步、看黑帧/水印）
### 9.2 自动验收（verify_sync.py + check_delivery.py + tail_guard.py，§9.2 清单）：音画偏移>0.3s FAIL；字幕重叠 FAIL；骨架段检出字幕 FAIL；片尾彩蛋混入 FAIL；封面黑帧 FAIL；时长超 band FAIL。
### 9.3 防剧透插卡复检
### 9.4 封面（cover_loop.py，禁黑帧禁字，纯图）
### 9.5 字幕防重叠
### 9.6 剪辑范围复检
### 9.7 骨架场次多样性复检（v13.3）

## 10. B站发布包（三件套）
`<片名>_精华版_<日期>.mp4` + `<片名>_封面_<日期>.jpg` + `<片名>_发布包_<日期>.md`（标题≤40字、简介150-220字、标签、分区影视剪辑；**禁提模型/技能名**；发布文案强制联网核实剧情与人名）。OSS 上传与剪贴板投递为个人服务，文档在包外（docs_private/），开源版跳过此步：直接本地交付。

## 11. 常见坑（精选）
### 通用
- ffmpeg 中文/引号路径炸 → Python subprocess 列表参数；heredoc 中文路径不可靠（实测 patch 含中文 old_string 偶发不匹配）→ 长中文文档改动用 python 精确替换+计数断言
- 字幕提前/滞后/重叠→§9.5；正片误加淡入淡出→§1；时间泄露→§4；彩蛋混入→§2.5；封面黑帧→§9.4
- 骨架同场景连排→§2.4；报幕词→§1；口水解说→§5；TTS 破折号读不出→§2.7
- 长会话遗忘→一切落盘（§7.5 上下文纪律）；批量中断→先读 batch_list.md 与磁盘产物再动手；禁把上部的镜号/人名带进本部
### v14.1 实测新增（抓特务 141min 首战）
- **vosk 中文路径坑**：Model() 的 C++ 层不吃非 ASCII 路径（模型文件齐全照样报错）→ full_asr/tail_guard 内置 `_ascii_model_path()`，在系统临时目录建 ASCII junction 后加载（脚本已自动，勿手改路径）。
- **Bellona≠cosyvoice**：cosyvoice-v3.5-plus 挂 Bellona 报 **HTTP 418**（燕铮莺只属 qwen3-tts 线）；两线音色表不通用，见 §2.7 引擎事实。
- **qwen3-tts 流式帧坑**：每 chunk 是独立 base64 整 wav（各自带 RIFF 头、首帧 size 字段占位）→ 跨 chunk 拼字符串再 decode 会得假时长（实测 44739s）；必须逐 chunk 剥头拼 PCM 重写头（tts_bailian.py 已内置）。
- **ffprobe/MSYS 路径**：bash 里 `/e/中文/x.mp4` 喂给 ffprobe 报 No such file → 一律传 `E:/中文/x.mp4` Windows 正斜杠形态（或走 Python subprocess）。
- **drawtext fontconfig 缺失 → ffmpeg segfault(exit 139)**：打标/蒙太奇一律走 PIL（contact_sheet.py 的 find_font 模式），别用 drawtext。
- **宽银幕源片拉伸变形**：1920×808 直拼 1080p 会拉扁 → cut_sync `--vf` letterbox 透传（§3），实测 scale+pad 方案有效。
- **60s 接触表锚点系统性偏移**：全片 60s 间隔墙只看个大概，定切点必翻车（实测 10/16 段错位）→ 切点必须 ①snap 吸附 ②**cutprobe_sheet 32 点目核** ③疑点用 5s 窗口 contact_sheet 重锚，三步缺一不可（§3.8 定剪前硬流程）。
- **tail_guard 把自家水印当残留文字 FAIL**：outro/水印含中文品牌词时 OCR 误报 → `--brand-ok "品牌词[,词2]"` 白名单（纯品牌帧豁免）；复检区间只量正片内容段，不含品牌落版。
- **check_delivery 误判原片时长**：文案里"141分钟的原片"是素材描述不是成片声明 → 上下文含"原片|原作|原剧|原著|小说|版本"自动豁免；转场卡命名 card_N 已计入 trans 计数。
- **解说词引台词违规**（§2.2）：卡文本里"我当警察怎么就不能娶你"这类原片台词直接引用=违规，写作阶段就会埋雷 → 渲染前对全部 texts.json 跑引号对白扫描，改写为画面链转述+诗意留白。
- **assemble 前台超时**：1200s 渲染超 terminal 前台 600s 上限 → 组装/水印/首扫 snap 一律 background=true 起，落日志文件轮询。
- **sigcache 复用**：snap_cuts 首扫全片建 `.sigcache.json`（~9min），同片复跑秒级命中——修切点重 snap 不重复付费；源文件改名/移动后缓存失效。

## 12. 工具脚本（scripts/，全部实测在本包内；按流程步骤排）
| 步骤 | 脚本 | 作用 |
|---|---|---|
| 1 | `contact_sheet.py` | fps 采样 + PIL 拼带时间戳 sheet（v13.3 `--scenes` 场景切分先验） |
| 3 | `keyframes.py` | 全片关键帧表 + 起/止 snap 计算（黑场免疫与片尾前向 snap） |
| 3.5 | `op_black_scan.py` | 1fps 色彩度量扫描 OP/黑场/logo 区间（低方差纯色兜底；memmap 省内存） |
| 3.8 | `snap_cuts.py` | v13 切点吸附：±3s 就近吸附天然接缝，出点宁晚不早；v13.2 `<video>.sigcache.json` 信号缓存 + no_seam 自动后扩≤6s |
| 3.85 | `cutprobe_sheet.py` | v13.2 切点探针（定剪前硬流程）：逐点实时抽帧拼真时间标签 sheet，目核全部切点 |
| 3.9 | `render_prologue.py` | v13 故事脉络片头：config.json 驱动，音频时长反推时间线，分辨率自适应 |
| 4/5 | `cut_sync.py` | 分段截取（切香肠式）：单命令同切 v/a、解码重编码，杜绝关键帧回吸 |
| 7 | `tts_batch.py` | TTS 批量合成（§2.7：超时兜底+三音色 fallback+断点续传+manifest 音色统一） |
| 7 | `tts_bailian.py` | **v14.1 主力 TTS**：百炼 qwen3-tts-flash/Bellona（逐帧解码+重试+manifest 记账） |
| 0 | `full_asr.py` | 全片离线 ASR（vosk-zh→台词本.txt/json，秒级时间戳；中文路径自动 junction） |
| 8 | `render_transition.py` | PIL 透明长条 + overlay 滚动转场视频 |
| 9 | `cover_loop.py` | 封面图 → 3s 静音片头 loop（v14 §9.4 防黑帧选帧内建） |
| 10 | `assemble.py` | 归一化拼接（v8 帧网格量化金标准；v12 品牌三缝合入；v13.2 分辨率锁定） |
| 11 | `verify_sync.py` | 音画同步三级法（L1 时长/L2 PTS/L3 内容级） |
| 11+ | `qc_env_check.py` | v10 B类累积漂移判级（50Hz 包络相关，三件套判级） |
| 11+ | `sync_marker_measure.py` | beep+闪白同步标记采样级实测（最强校验） |
| 11++ | `tail_guard.py` | v13.1 交付前自动尾巴复检（防剧透/混入自检） |
| 12 | `apply_watermark.py` | v12.1 水印窗口：头/尾各 3min 叠右上水印，中间干净 |
| 12.5 | `check_delivery.py` | v13.2 交付数字自检：三件套文案名场面数/解说章数/总时长 vs order 实盘 |
| — | `ad_scan.py` | 段内中插广告扫描（时间压缩剧） |
品牌资产：`brand/render_brand_v2.py`（定版，禁改）+ 预渲染 intro/outro_{720p,1080p,4k}.mp4、wm_*.png。ASR：`models/vosk-zh/`（66MB，断网兜底）。
所有脚本用 managed venv Python（含 PIL/numpy/requests/edge-tts/vosk）运行；中文路径一律 Python subprocess 传绝对路径列表参数，禁 bash 裸调。

## 13. 决策留痕
每部片 `output/剪辑报告.md` 记录：假设与自行决定点、镜号表、每卡文本、验收结果与修复轮次、blocked/with-issue 原因。用户复核只看报告+台账即可追溯全程。

---

## 附：任务提示词（v14 唯一版——开一部片/一批片时整段复制粘贴）

> 用法：直接粘贴下面整段给任意会话（不分模型），替换 ①–④ 占位。方案已白天定案的填路径，未定案的填"无"（会话自行读片定案）。一个会话从 ① 跑到交付。

```
【角色】你是影视精华版剪辑执行师。先完整读取技能包规范：
<技能包根>\SKILL.md
（v14 单会话全流程批处理）。全程零提问、失败重试1次后跳过不卡死、断点续跑靠磁盘产物与 batch_list.md 台账。

【本片参数】
① 片源 = ____（用户片源目录下的视频文件；生产目录自动用包内 projects\<片名>\）
② 人声 = ____（默认百炼 tts_bailian.py voice=Bellona 燕铮莺；克隆声 voice_id 填这里，走 §2.7 备胎通道）
③ 已定案解说方案 = ____（plan/故事脉络.md/卡文本路径；填"无"=你按 §0 完整读片后自行产出四幕方案与解说文本再执行）
④ 批量 = ____（单部填片名；多部逐部循环，先建/更新台账）

【执行顺序（一步不落，每步落盘再下一步）】
§0 时长探针+完整读片(0-10min 常速) → work/sheet/故事脉络.md
§1–§5 四幕骨架+转场卡（每镜10-20s每幕70-80s、多场次≥4、卡文本250-350字独立成文、禁报幕词/时间码/破折号）
§2.7 TTS(tts_bailian.py→句级wav+实测时长回写时序) → §3 选镜+cutprobe_sheet+snap_cuts
§6 组装(cut_sync→assemble 品牌三缝→render_prologue片头→卡段SRT字幕，骨架零字幕)
§2.5/§8 彩蛋三查+防剧透插卡 → §9.1/9.2 自动验收(verify_sync+check_delivery+tail_guard，FAIL修复≤3轮)
§9.4 封面(禁黑帧禁字) → §10 联网核实+发布包三件套(标题≤40/简介150-220字/禁提模型技能名)
→ output/ 三件套 + 剪辑报告.md(§13 决策留痕) → 台账标 done → 交付清单(MEDIA: 链接)。

【边界】交付=B站三件套清单为止；OSS/clipboard 未配置则本地交付；一切人工平台操作不代办不承诺。
【收尾回复】只报：成片路径/实测时长/质检结论/blocked原因(如有)/三件套清单。
```
